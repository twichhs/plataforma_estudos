{
  "name": "trajeto-qa",
  "private": true,
  "description": "Suíte de QA da plataforma Trajeto (a aplicação em si não tem dependências)",
  "scripts": {
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test unit.test.mjs",
    "test:e2e": "node --test --test-timeout=120000 e2e.test.mjs"
  },
  "devDependencies": {
    "puppeteer-core": "^23.0.0"
  }
}
